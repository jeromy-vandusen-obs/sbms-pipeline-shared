String call(String host, String port) {
    // temporarily disabling this to work on the pipeline without tests.....
//    if (! waitUntilActive(host, port, 10, 30)) {
//        return 'FAILURE'
//    }
    return 'SUCCESS'
}
